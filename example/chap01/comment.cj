main() {
    // 这是一个单行注释
    println("Hello World!") // 这是另一个单行注释

    // println("Hello World!") // 这行代码被注释掉了，不会执行

    /*
     * 这是一个多行注释
     * 它可以跨越多行
     *
     * 前面的 * 只是为了好看，不是必须的
     */
    println("你好仓颉！")
}
